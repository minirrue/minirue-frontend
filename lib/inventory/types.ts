/**
 * Inventory domain — shared types.
 * Re-exported from lib/inventory/api.ts; kept here for components that
 * need types without the fetch functions.
 */

export type { StockAdminRow, StockStatus, MovementRow, MovementType, WarehouseRow } from './api';
